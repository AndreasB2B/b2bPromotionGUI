@page
@model KN.B2B.Web.Pages.Private.Masterdata.CancellationReasons.EditModel

@{
    ViewData["Title"] = "Edit Cancellation Reason";
}

<h1>Edit</h1>

<h4>Cancellation Reason</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CancellationReason.Id" />
            <div class="form-group">
                <label asp-for="CancellationReason.Reason" class="control-label"></label>
                <input asp-for="CancellationReason.Reason" class="form-control" />
                <span asp-validation-for="CancellationReason.Reason" class="text-danger"></span>
            </div>
            <hr />
            <div>
                <button id="save" class="btn btn-success" type="submit">
                    <i class="fas fa-save"></i>
                    Save
                </button>
                |
                @if (Model.CancellationReason.Id == 0)
                {
                    <button disabled class="btn btn-danger" type="reset">
                        <i class="fas fa-trash-alt"></i>
                        Delete
                    </button>
                }
                else
                {
                    <a class="btn btn-danger" type="reset" asp-page="./Delete" asp-route-id="@Model.CancellationReason.Id">
                        <i class="fas fa-trash-alt"></i>
                        Delete
                    </a>
                }
                |
                <a class="btn btn-secondary" type="reset" asp-page="./Index">
                    <i class="fa fa-list"></i>
                    Back
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/js/unsaved.js" asp-append-version="true"></script>
}
